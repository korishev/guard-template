This is a simple repo for creating a guard plugin from scratch.

replace all occurrences of "template" with the name of your class, and
move the /lib/guard/template directory to your /lib/guard/classname.
